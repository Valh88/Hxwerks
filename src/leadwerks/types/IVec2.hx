package leadwerks.types;

import leadwerks.Globals;

/**
	Integer 2D vector (iVec2) from Leadwerks API (used for GraphicsMode, size, position, etc.).
**/
typedef IVec2 =
{
	var x:Int;
	var y:Int;
}
